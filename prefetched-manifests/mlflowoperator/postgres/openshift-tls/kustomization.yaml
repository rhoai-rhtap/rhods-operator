apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# Chain: base → openshift (PGDATA patch) → openshift-tls (TLS component)
resources:
  - ../openshift

components:
  - ../components/tls
